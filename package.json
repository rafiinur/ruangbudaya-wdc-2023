{
  "devDependencies": {
    "tailwindcss": "^3.3.5"
  },
  "scripts": {
    "build": "tailwindcss build -i ./input.css -o dist/output.css",
    "watch": "tailwindcss build -i ./input.css -o dist/output.css --watch"
  }
}
